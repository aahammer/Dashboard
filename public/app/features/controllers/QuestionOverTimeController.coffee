define(['d3', 'angular'], (d3, angular) ->


    ($settings) ->
        ChartController = ($rootScope, $scope, $window, DataService, $settings) ->


            return

        return ['$rootScope' , '$scope', '$window', 'DataService', $settings, ChartController ]

)